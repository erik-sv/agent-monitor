# Marketing Intel Sources
# Enable or disable individual sources. Set to 1 to enable, 0 to disable.
# Each source corresponds to a Python script in the sources/ directory.
#
# To add a new source:
#   1. Create sources/<name>.py implementing the standard interface
#   2. Add <name>=1 below
#   3. Add any required API keys to .env

ga4=1
gsc=1
zoho_salesiq=1
